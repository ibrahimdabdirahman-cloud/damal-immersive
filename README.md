# Damal Restaurant — Immersive

Standalone static site (design variant: Immersive). Self-contained design-canvas runtime, no build step. Forms wired to the shared Masul forms backend.

## Deploy
Static site — push to GitHub and connect to Vercel (Framework: Other).
